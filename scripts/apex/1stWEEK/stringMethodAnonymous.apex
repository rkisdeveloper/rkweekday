StringMethodsDemo sm = new StringMethodsDemo();
sm.stringMethod5();
sm.stringMethod4();
sm.stringMethod3();
sm.stringMethod2();
sm.stringMethod1();

Decimal d = 55.334;
string s = string.valueOf(d);

// '+' -> when we use + with string it will concate.
//        when we use + with integer, decimal, long, double it will add

system.debug(d+5);
system.debug(s+5);